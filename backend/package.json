{
  "name": "leadbot-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute leadbot-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}